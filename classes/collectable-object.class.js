/**
 * Represents a collectable object that extends the DrawableObject class.
 * @extends DrawableObject
 */
class CollectableObject extends DrawableObject {
  /**
   * Creates an instance of CollectableObject.
   */
  constructor() {
    super();
  }
}
